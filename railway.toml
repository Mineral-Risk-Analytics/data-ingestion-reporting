[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
# Railway sets $PORT automatically — the Dockerfile CMD uses it.
healthcheckPath = "/api/v1/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
